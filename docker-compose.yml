services:
  app:
    build: .
    ports:
      - "8081:8080"
    volumes:
      - .:/app
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: redis:7
    ports:
      - "6380:6379"